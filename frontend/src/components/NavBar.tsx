import NavItem from "./NavItem"

const NavBar = () => {
    return <nav>
        <NavItem to="/">
            Products
        </NavItem>
        <NavItem to="/">
            Explore
        </NavItem>
        
    </nav>
}

export default NavBar
